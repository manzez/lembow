# Notification Service Configuration

# Server Configuration
NOTIFICATION_PORT=5001

# Email Provider Configuration
# Options: 'gmail', 'smtp', 'sendgrid', or leave empty for development mode
EMAIL_PROVIDER=development

# Gmail Configuration (if using Gmail)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# SMTP Configuration (if using custom SMTP)
SMTP_HOST=smtp.your-provider.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password

# SendGrid Configuration (if using SendGrid)
SENDGRID_API_KEY=your-sendgrid-api-key

# Email Settings
FROM_EMAIL=noreply@lembow.com
SUPPORT_EMAIL=support@lembow.com

# Application URLs
APP_URL=http://localhost:3000
API_URL=http://localhost:4001